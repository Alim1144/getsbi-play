# Быстрая инструкция для Render

## 1. Настройка GitHub (если еще не сделано)

Если у вас еще нет репозитория на GitHub:

1. Перейдите на https://github.com/new
2. Название: `getsbi-play` (или любое другое)
3. Оставьте репозиторий пустым (не добавляйте README)
4. Нажмите "Create repository"
5. Скопируйте URL репозитория

Затем выполните:

```bash
git remote add origin https://github.com/ВАШ-USERNAME/getsbi-play.git
git branch -M main
git push -u origin main
```

## 2. Настройка Render

1. Откройте https://dashboard.render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub аккаунт (если еще не подключен)
4. Выберите репозиторий `getsbi-play`

### Настройки сервиса:

**Basic Settings:**
- Name: `getsbi-play`
- Region: выберите ближайший
- Branch: `main`
- Root Directory: оставьте пустым

**Build & Deploy:**
- Environment: `Node`
- Build Command: `npm install && npm run build`
- Start Command: `npm start`

**Environment Variables:**
Добавьте две переменные:
- Key: `TELEGRAM_BOT_TOKEN`, Value: `7953146305:AAEjT4qYVI7zciK0bx1b2U0Ww41UZUaA-cY`
- Key: `TELEGRAM_CHAT_ID`, Value: `6920795432`

5. Нажмите "Create Web Service"

## 3. Ожидание деплоя

- Первая сборка займет 3-5 минут
- После завершения ваш сайт будет доступен по адресу: `https://getsbi-play.onrender.com` (или похожему)

## Готово! 🎉

Ваш интернет-магазин GETSBI play будет работать онлайн!
