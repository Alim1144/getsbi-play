# Настройка базы данных MongoDB

## Почему нужна база данных?

Сейчас товары хранятся в **localStorage** браузера, что означает:
- ❌ Товары видны только в том браузере, где их добавили
- ❌ Другие пользователи не видят ваши товары
- ❌ Товары могут исчезнуть при очистке кэша
- ❌ Не работает на нескольких устройствах

С базой данных MongoDB:
- ✅ Товары доступны всем пользователям
- ✅ Данные сохраняются на сервере
- ✅ Работает на всех устройствах
- ✅ Надежное хранение данных

## Шаг 1: Создание MongoDB Atlas аккаунта

1. Перейдите на https://www.mongodb.com/cloud/atlas/register
2. Зарегистрируйтесь (можно через Google)
3. Создайте бесплатный кластер (M0 - Free tier)

## Шаг 2: Настройка кластера

1. Выберите провайдера: **AWS** (рекомендуется)
2. Выберите регион: ближайший к вам (например, **Frankfurt** для Европы)
3. Нажмите "Create Cluster" (создание займет 3-5 минут)

## Шаг 3: Создание пользователя базы данных

1. В разделе "Security" → "Database Access"
2. Нажмите "Add New Database User"
3. Выберите "Password" аутентификацию
4. Введите имя пользователя и пароль (запомните их!)
5. Выберите "Atlas Admin" роль
6. Нажмите "Add User"

## Шаг 4: Настройка сетевого доступа

1. В разделе "Security" → "Network Access"
2. Нажмите "Add IP Address"
3. Нажмите "Allow Access from Anywhere" (0.0.0.0/0) для начала
   - ⚠️ Для продакшена лучше указать IP адрес Render сервера
4. Нажмите "Confirm"

## Шаг 5: Получение строки подключения

1. В разделе "Database" → "Connect"
2. Выберите "Connect your application"
3. Скопируйте connection string (выглядит как):
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
4. Замените `<username>` и `<password>` на ваши данные
5. Добавьте имя базы данных в конце: `?retryWrites=true&w=majority` → `?retryWrites=true&w=majority&appName=getsbi-play`

## Шаг 6: Добавление в Environment Variables на Render

1. Перейдите в ваш сервис на Render
2. Откройте раздел "Environment"
3. Добавьте новую переменную:
   - **Key**: `MONGODB_URI`
   - **Value**: ваша строка подключения (с username и password)

## Шаг 7: Установка зависимостей

После обновления кода выполните:
```bash
npm install
```

## Готово! 🎉

После деплоя на Render товары будут храниться в MongoDB и будут доступны всем пользователям.

## Важные замечания

- **Бесплатный tier MongoDB Atlas** предоставляет 512 MB хранилища (достаточно для начала)
- Для продакшена рассмотрите настройку индексов для быстрого поиска
- Регулярно делайте резервные копии данных
- Для безопасности в продакшене ограничьте IP адреса в Network Access

